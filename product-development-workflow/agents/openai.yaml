interface:
  display_name: "产品开发工作流"
  short_description: "从模糊想法到有证据支持的开发交付"
  default_prompt: "使用 $product-development-workflow 先只读分流并反馈当前理解；阶段三形成完整需求时先请求 requirements.md 写入授权，阶段四、五产物的内容、状态和路径确定后直接写入 spec.md 或 implementation-plan.md，不再申请 Markdown 写入权限；实施代码仍需另行授权。"
