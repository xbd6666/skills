# AGENTS.md

## 适用范围

本文件适用于 `{RepositoryRoot}` 下的所有文件。

## 工作前必读

修改任何文件前：

1. 阅读根目录 `CONVENTIONS.md`。
2. 按从仓库根到目标目录的顺序阅读所有适用的 `CONVENTIONS.md` 与 `AGENTS.md`。
3. 无法确认文档冲突时先报告，不自行选择更方便的规则。

`CONVENTIONS.md` 维护长期规则；本文件只提供执行入口和高风险护栏，无权覆盖长期规则。

## 模块规则路由

| 目标路径 | 必读约定 | 模块入口 |
| --- | --- | --- |
| `{ModulePath}` | `{ModuleConventionPath}` | `{ModuleAgentsPathOrNone}` |

## 仓库职责

`{RepositoryName}` 是 `{RepositoryRole}`，负责 `{PrimaryResponsibility}`。

## 高风险执行护栏

- {RiskGuardrailOne}
- {RiskGuardrailTwo}
- 不要 {ForbiddenAction}

## 验证要求

- {VerificationRule}
- 明确区分局部静态检查、完整构建、真实数据库、外部系统和部署证据。
