# AGENTS.md

## 适用范围

本文件适用于 `{RepositoryRoot}` 下的所有文件。

## 工作前必读

修改任何文件前：

1. 阅读根目录 `CONVENTIONS.md`。
2. 按从仓库根到目标目录的顺序阅读所有适用的 `CONVENTIONS.md` 与 `AGENTS.md`。
3. 无法确认文档冲突时先报告，不自行选择更方便的规则。

各主题按 `CONVENTIONS.md` 中声明的权威来源维护；本文件只提供执行入口及有来源的提醒，不另行定义或覆盖长期规则。

## 模块规则路由

| 目标路径 | 必读约定 | 模块入口 |
| --- | --- | --- |
| `{ModulePath}` | `{ModuleConventionPath}` | `{ModuleAgentsPathOrNone}` |

## 仓库职责

`{RepositoryName}` 是 `{RepositoryRole}`，负责 `{PrimaryResponsibility}`。职责来源：{ResponsibilitySourceAndTopic}。

## 高风险执行提醒

| 动作或风险 | 执行要求摘要 | 已确认权威来源 |
| --- | --- | --- |
| {ActionOrRisk} | {ConfirmedRequirementReminder} | {CanonicalDocumentAndTopic} |

## 验证入口

- 验证规则及命令执行信息：{VerificationSourceAndTopic}。
- 报告实际执行结果及未覆盖范围。
