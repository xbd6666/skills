# AGENTS.md

## 适用范围

本文件适用于 `{ScopePath}` 下的所有文件。

## 工作前必读

修改本目录下任何文件前，先阅读并遵守同目录的 `CONVENTIONS.md`。如果本文件和 `CONVENTIONS.md` 有冲突，以 `CONVENTIONS.md` 为准。

## 项目/模块职责

`{ScopeName}` 是 `{ScopeRole}`，负责 `{PrimaryResponsibility}`。

- `{DoRuleOne}`
- `{DoRuleTwo}`
- 不要 `{DontRuleOne}`
- 不要 `{DontRuleTwo}`

## 高风险规则

- `{RiskRuleOne}`
- `{RiskRuleTwo}`

## 验证要求

- `{VerificationRule}`
