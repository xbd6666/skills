# AGENTS.md

## 适用范围

本文件适用于 `{ModulePath}` 下的所有文件。

## 规则读取顺序

修改前，按从仓库根到本目录的顺序阅读：{ApplicableDocumentChain}。

- 父级执行护栏继续适用；本文件只能增加或收紧局部护栏，不能放宽父级限制。
- 同目录 `CONVENTIONS.md` 维护本模块的长期规则和显式覆盖。
- 本文件无权创建或覆盖长期规则；需要改变长期规则时，更新同目录 `CONVENTIONS.md`。
- 无法同时满足适用规则时，停止受影响修改并报告冲突。

## 模块职责

`{ModuleName}` 是 `{ModuleRole}`，负责 `{PrimaryResponsibility}`。

- 应当 {DoRuleOne}
- 应当 {DoRuleTwo}
- 不要 {DontRuleOne}
- 不要 {DontRuleTwo}

## 局部高风险护栏

- {RiskGuardrailOne}
- {RiskGuardrailTwo}

## 验证要求

- {VerificationRule}
- 报告本模块验证与仓库级验证的边界。
