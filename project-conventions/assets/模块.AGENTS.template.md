# AGENTS.md

## 适用范围

本文件适用于 `{ModulePath}` 下的所有文件。

## 规则读取顺序

修改前，按从仓库根到本目录的顺序阅读：{ApplicableDocumentChain}。

- 父级规则摘要按本模块有效继承结果解释，不抵消合法覆盖；独立执行护栏按来源规定的范围和覆盖条件生效，不以其他规则的覆盖附带解除。
- 本模块长期规则、专业文档权威关系和显式覆盖见同目录 `CONVENTIONS.md`。
- 本文件不另行创建或覆盖长期规则；需要改变长期规则时，更新其权威文档并在此引用。
- 无法同时满足适用规则时，停止受影响修改并报告冲突。

## 模块职责

`{ModuleName}` 是 `{ModuleRole}`，负责 `{PrimaryResponsibility}`。职责与协作边界来源：{ResponsibilitySourceAndTopic}。

## 局部高风险执行提醒

| 动作或风险 | 执行要求摘要 | 已确认权威来源 | 适用范围与覆盖条件 |
| --- | --- | --- | --- |
| {ActionOrRisk} | {ConfirmedRequirementReminder} | {CanonicalDocumentAndTopic} | {EffectiveRuleScopeAndOverrideConditions} |

## 验证入口

- 验证规则及命令执行信息：{VerificationSourceAndTopic}。
- 报告实际执行结果、本模块覆盖范围及仓库级验证边界。
