## 项目/模块组织

`{ScopeName}` 是 `{ScopeRole}`，负责 `{PrimaryResponsibility}`。

```
{ScopePath}/
├── {Folder}/                    # {FolderResponsibility}
└── CONVENTIONS.md
```

## 职责边界

- `{ScopeName}` 负责 `{AllowedResponsibility}`
- 不要在本范围放 `{ForbiddenResponsibility}`
- 需要 `{ExternalCapability}` 时，通过 `{DependencyBoundary}` 处理

## 依赖与协作方向

```
{DependencyOrCollaborationDirection}
```

## 核心约定

- `{RuleOne}`
- `{RuleTwo}`
- `{RuleThree}`

## 新增能力的标准步骤

1. `{StepOne}`
2. `{StepTwo}`
3. `{StepThree}`

## 验证方式

- `{VerificationCommandOrCheck}`

## 编码与文档规范

- `{CodingRuleOne}`
- `{DocumentationRuleOne}`
