# {RepositoryName} 项目约定

## 适用范围

- 本文适用于：{RepositoryRoot}
- 本文汇总仓库级规则及其权威来源，各主题按下表分工维护。

## 工程文档权威关系

| 主题 | 权威来源 | 本文处理方式 |
| --- | --- | --- |
| {Topic} | {CanonicalDocumentOrConfig} | 直接定义/引用/仅说明边界 |

## 仓库定位与结构

`{RepositoryName}` 是 `{RepositoryRole}`，主要负责 `{PrimaryResponsibility}`。

```text
{RepositoryRoot}/
├── {ModulePath}/               # {ModuleResponsibility}
├── CONVENTIONS.md
└── AGENTS.md
```

## 模块职责与协作边界

| 模块 | 负责 | 不负责 | 允许依赖 |
| --- | --- | --- | --- |
| `{ModulePath}` | {OwnedResponsibility} | {ForbiddenResponsibility} | {AllowedDependencies} |

## 跨模块与公共契约

- {CrossModuleRule}
- {PublicContractRule}
- {CompatibilityRule}

## 新增能力的标准步骤

1. {StepOne}
2. {StepTwo}
3. {StepThree}

## 配置、数据与安全

- {ConfigurationRule}
- {DataOrPersistenceRule}
- {SecurityOrPrivacyRule}

## 生成物与外部系统

- {GeneratedArtifactRule}
- {ExternalSystemBoundary}

## 验证要求

| 工作目录（相对仓库根） | 命令或检查入口 | 前置条件 | 验证范围 | 副作用或产物 | 依据与状态 |
| --- | --- | --- | --- | --- | --- |
| {VerificationWorkingDirectory} | {StableVerificationCommandOrCheck} | {VerificationPrerequisitesOrNone} | {VerifiedScope} | {VerificationEffectsOrNone} | {VerificationEvidenceAndStatus} |

- {VerificationBoundary}

## 编码与文档维护

- {CodingOrNamingRule}
- {DocumentationRule}
- 长期规则在上表指定的权威文档或配置中维护；本文承载分配给自身的项目约定，执行入口引用对应来源，不重复维护另一套规则。
- 父规则变更前后，检查受影响子级文档、覆盖项和规则引用；必要联动修改超出已授权范围时，先报告准确文件和影响，确认范围后再应用关联变更。

## 待确认事项（不生效）

本节内容尚未形成已确认规则，不作为执行要求。

| 主题 | 候选内容或未决问题及适用范围 | 证据与反例 | 确认点 |
| --- | --- | --- | --- |
| {PendingTopic} | {PendingRuleOrQuestionAndScope} | {PendingEvidenceAndCounterexample} | {ConfirmationNeeded} |
