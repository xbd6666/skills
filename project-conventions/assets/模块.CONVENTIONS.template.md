# {ModuleName} 模块约定

## 适用范围与继承

- 本文适用于：{ModulePath}
- 上级约定：{ParentConventionPath}
- 除有效覆盖明确改变的规则部分和适用范围外，继续遵守全部上级约定，包括同一主题下的其他要求。
- 局部扩展与上级规则叠加，不免除上级要求。
- 本模块其他主题的权威来源：{ModuleAuthoritySourcesOrNone}

## 局部扩展

| 主题 | 当前模块新增规则 | 依据 |
| --- | --- | --- |
| {ExtendedTopic} | {LocalRule} | {Evidence} |

## 覆盖项

没有覆盖时写“无”。存在覆盖时，每项完整记录：

| 上级文档 | 上级主题 | 上级规则 | 当前规则 | 覆盖理由 | 适用边界 |
| --- | --- | --- | --- | --- | --- |
| {ParentConventionPath} | {ParentTopic} | {ParentRule} | {LocalOverride} | {OverrideReason} | {OverrideScope} |

## 模块定位与结构

`{ModuleName}` 是 `{ModuleRole}`，负责 `{PrimaryResponsibility}`。

```text
{ModulePath}/
├── {Folder}/                   # {FolderResponsibility}
├── CONVENTIONS.md
└── AGENTS.md
```

## 职责与依赖边界

- 本模块负责：{AllowedResponsibility}
- 本模块不负责：{ForbiddenResponsibility}
- 需要 `{ExternalCapability}` 时，通过 `{DependencyBoundary}` 协作。

```text
{DependencyOrCollaborationDirection}
```

## 公共契约与兼容性

- {PublicContractRule}
- {CompatibilityRule}

## 新增能力的标准步骤

1. {StepOne}
2. {StepTwo}
3. {StepThree}

## 配置、数据与安全

- {ConfigurationRule}
- {DataOrPersistenceRule}
- {SecurityOrPrivacyRule}

## 验证要求

| 工作目录（相对仓库根） | 命令或检查入口 | 前置条件 | 验证范围 | 副作用或产物 | 依据与状态 |
| --- | --- | --- | --- | --- | --- |
| {VerificationWorkingDirectory} | {StableVerificationCommandOrCheck} | {VerificationPrerequisitesOrNone} | {VerifiedScope} | {VerificationEffectsOrNone} | {VerificationEvidenceAndStatus} |

- {VerificationBoundary}

## 编码与文档维护

- {CodingOrNamingRule}
- {DocumentationRule}
- 上级或本模块规则变更时，复核相关覆盖项、受影响子级文档和规则引用；仅重命名时更新引用并保留覆盖语义，内容变化或实际删除时重新判断覆盖是否成立。
- 必要联动修改超出已授权范围时，先报告准确文件和影响，确认范围后再应用关联变更。

## 待确认事项（不生效）

本节内容尚未形成已确认规则，不作为执行要求。

| 主题 | 候选内容或未决问题及适用范围 | 证据与反例 | 确认点 |
| --- | --- | --- | --- |
| {PendingTopic} | {PendingRuleOrQuestionAndScope} | {PendingEvidenceAndCounterexample} | {ConfirmationNeeded} |
