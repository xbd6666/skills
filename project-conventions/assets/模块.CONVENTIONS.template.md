# {ModuleName} 模块约定

## 适用范围与继承

- 本文适用于：{ModulePath}
- 上级约定：{ParentConventionPath}
- 未在本文扩展或覆盖的主题继续遵守上级约定。

## 局部扩展

| 主题 | 当前模块新增规则 | 依据 |
| --- | --- | --- |
| {ExtendedTopic} | {LocalRule} | {Evidence} |

## 覆盖项

没有覆盖时写“无”。存在覆盖时，每项完整记录：

| 上级文档 | 上级主题 | 上级规则 | 当前规则 | 覆盖理由 | 适用边界 |
| --- | --- | --- | --- | --- | --- |
| {ParentConventionPath} | {ParentTopic} | {ParentRule} | {LocalOverride} | {OverrideReason} | {OverrideScope} |

## 模块定位与结构

`{ModuleName}` 是 `{ModuleRole}`，负责 `{PrimaryResponsibility}`。

```text
{ModulePath}/
├── {Folder}/                   # {FolderResponsibility}
├── CONVENTIONS.md
└── AGENTS.md
```

## 职责与依赖边界

- 本模块负责：{AllowedResponsibility}
- 本模块不负责：{ForbiddenResponsibility}
- 需要 `{ExternalCapability}` 时，通过 `{DependencyBoundary}` 协作。

```text
{DependencyOrCollaborationDirection}
```

## 公共契约与兼容性

- {PublicContractRule}
- {CompatibilityRule}

## 新增能力的标准步骤

1. {StepOne}
2. {StepTwo}
3. {StepThree}

## 配置、数据与安全

- {ConfigurationRule}
- {DataOrPersistenceRule}
- {SecurityOrPrivacyRule}

## 验证要求

- {StableVerificationCommandOrCheck}
- {VerificationBoundary}

## 编码与文档维护

- {CodingOrNamingRule}
- {DocumentationRule}
