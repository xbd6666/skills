interface:
  display_name: "DBX MCP"
  short_description: "Use DBX MCP for database work"
  default_prompt: "Use $dbx-mcp to inspect my DBX connections and help write a safe SQL query."
dependencies:
  tools:
    - type: "mcp"
      value: "dbx"
      description: "DBX database MCP server"
policy:
  allow_implicit_invocation: true
